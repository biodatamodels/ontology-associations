create table gafassociation (
  db text,
  local_id text,
  db_object_symbol text,
  relation text,
  ontology_class_ref text,
  supporting_references text,
  evidence_type text,
  with_or_from text,
  aspect text,
  db_object_name text,
  db_object_synonyms text,
  db_object_type text,
  db_object_taxon text,
  annotation_date text,
  assigned_by text,
  annotation_extensions text,
  gene_product_form text
);
